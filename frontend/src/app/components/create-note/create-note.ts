import { Component } from '@angular/core';

// Este componente ha sido reemplazado por app-note-form.
// Se mantiene para no romper la estructura de carpetas existente.
@Component({
  selector: 'app-create-note',
  standalone: true,
  imports: [],
  template: '',
})
export class CreateNote {}
